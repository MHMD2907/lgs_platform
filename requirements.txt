streamlit>=1.35
pandas>=2.0
pdfplumber>=0.11
pypdfium2>=4.18   # PDF sayfalarını resme çevirme + hızlı metin okuma. pdfplumber
                  # bunu dolaylı olarak kuruyordu ama kod DOĞRUDAN kullanıyor;
                  # açıkça yazılmazsa bir gün sessizce kaybolabilir.
PyPDF2>=3.0
pycryptodome>=3.20   # MEB'in resmi LGS PDF'leri şifreli (AES) olduğu için gerekli
requests>=2.31
# Kalıcı veritabanı (Supabase/PostgreSQL) bağlantısı için. DB_URL ayarlanmamışsa
# hiç kullanılmaz; uygulama eskisi gibi yerel dosyayla çalışır.
psycopg[binary]>=3.1
Pillow>=10.0         # PDF'leri küçültmek (öğrenciye gösterilen kitapçığın boyutunu düşürmek) için

# Google Drive entegrasyonu için (opsiyonel; kullanmayacaksanız kurmasanız da olur)
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2
pikepdf>=8.0         # PDF'lerdeki ÖSYM/MEB filigranını (damga katmanını) kayıpsız silmek için

# Cevap anahtarı sayfası PDF'e YAZI olarak değil ÇİZİM/RESİM olarak
# gömülmüşse (ör. 2016 KPSS Lisans kitapçığı) sayfayı resimden okumak
# için. Yanında packages.txt'teki 'tesseract-ocr' de gerekir.
pytesseract>=0.3.10
